## AI Task Hub · Win32 便携版 0.3.4

本次发布使用独立的 `win32-v0.3.4` 标签，面向 Windows x64 原生便携版。

### 本版变化

- 修复 Windows 通知区域（系统托盘）图标点击无响应的问题。
- 兼容 `NOTIFYICON_VERSION_4` 的托盘事件编码，左键打开主面板、右键打开菜单。
- 更新健康接口与状态接口版本号为 `0.3.4-win32`，便于确认运行中的确切版本。

### 验证

- Win32 原生回归测试：71 项检查通过。
- 已完成真实窗口与托盘事件路径验证；压缩包附带 SHA-256 校验文件。

下载 `AI-Task-Hub-Win32-x64-0.3.4.zip` 后完整解压运行，并核对同名 `.sha256` 文件。
